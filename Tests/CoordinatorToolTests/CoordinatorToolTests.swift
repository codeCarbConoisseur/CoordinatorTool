import XCTest
@testable import CoordinatorTool

final class CoordinatorToolTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(CoordinatorTool().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
